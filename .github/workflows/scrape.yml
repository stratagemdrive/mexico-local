name: Mexico News RSS Scraper

on:
  schedule:
    # 6 AM EST = 11:00 UTC  |  6 PM EST = 23:00 UTC
    - cron: "0 11 * * *"
    - cron: "0 23 * * *"
  workflow_dispatch:  # Allow manual runs

jobs:
  scrape-news:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Create docs folder if missing
        run: mkdir -p docs

      - name: Run scraper
        run: python scraper.py
        env:
          OUTPUT_FILE: docs/mexico_news.json

      - name: Commit and push updated JSON
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add docs/mexico_news.json
          git diff --cached --quiet || git commit -m "chore: update docs/mexico_news.json [$(date -u '+%Y-%m-%d %H:%M UTC')]"
          git push
